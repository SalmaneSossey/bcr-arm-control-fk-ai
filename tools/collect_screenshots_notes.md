# Screenshot Notes

Capture these once Gazebo is running correctly:

- full robot view in Gazebo
- controller list in terminal
- motion test terminal output
- FK terminal output
- training loss plot
- evaluation terminal metrics

For the report, note the command used, the date, and which requirement each screenshot proves.

Current stored assets:

- `docs/media/gazebo_arm_render.png`
- `moving_arm.mp4`
- `bcr_ws/src/bcr_arm_gazebo/plots/fk_mlp_loss.png`
